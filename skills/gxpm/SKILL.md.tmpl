---
name: gxpm
description: Second-generation agent project management runtime. Use when user mentions gxpm, issue phases, worktrees, artifacts, checkpoint recovery, or asks about project management workflow.
---

{{PREAMBLE}}

# gxpm

gxpm 是面向代理执行的统一项目管理运行时。
产品目标是替代 PMC 和 gstack，以原生状态图、能力运行时和阶段门控推进 —— 不是包装它们。

## 角色

先读状态，再路由到正确阶段，调用正确的 gxpm 能力，
在声称进度前持久化 artifact。将 `.gxpm/issues/<id>/` 视为
单一真相源，而非聊天记忆或 Linear 评论。

## 速查手册

```bash
gxpm issue status <issue-id>          # 读取当前阶段
gxpm issue next <issue-id>            # 推荐下一步命令
gxpm issue context <issue-id>         # 完整上下文 + 恢复新鲜度 + 必读文件 + 代理指令（新会话续做的首选入口）
gxpm issue list                       # 活跃 issue
gxpm issue create --auto-id           # 使用下一个空闲 id 创建新 issue
gxpm doctor                           # 健康检查
gxpm version                          # 已安装版本
```

## 阶段地图

- `triage`: 澄清 issue、范围、风险、下一阶段。
- `plan`: 产出已批准的实现和验证计划。
- `dispatch`: 创建 worktree/交接/契约。
- `implement`: 工作者拥有的实现。
- `local-verify`: 代理拥有的本地验证证据。
- `ac-check`: 验收契约履行。
- `self-review`: PR 前内部审查。
- `ship`: PR/发布准备。
- `pr-check`: 审查评论和 PR 风险。
- `verify`: 独立验收验证。
- `qa`: 浏览器/运行时证明（如需要）。
- `land`: 合并/部署交接门。

## 必需习惯

- 绝不从聊天记忆推断阶段。
- 绝不跳过 artifact 回写。
- 绝不让 Linear 替代本地状态。
- 绝不未经用户明确确认就执行不可逆的 land 操作。
- 将 PMC/gstack 视为上游参考，而非最终运行时依赖。
- 优先使用 gxpm 原生能力，而非硬编码的主机假设。

{{REFERENCE:key-rules}}

## 相关 Skills

在任务需要时加载适当的 skill：

- `/gxpm-diagnose` — 纪律性调试循环
- `/gxpm-grill` — 对齐会议，精炼术语
- `/gxpm-tdd` — 垂直切片测试驱动开发
- `/gxpm-architecture` — 架构摩擦分析
- `/gxpm-planning` — PRD 合成与垂直切片拆分
- `/gxpm-triage` — issue 状态机管理
- `/gxpm-debug-issue` — GitNexus 驱动调试
- `/gxpm-explore-codebase` — GitNexus 驱动架构探索
- `/gxpm-refactor-safely` — GitNexus 驱动安全重构
- `/gxpm-review-changes` — GitNexus 驱动变更审查

可选的 wiki 操作：`gxpm wiki status/init/update/query`。
浏览器证据捕获：加载 `/gxpm-browser`。

## Read Next

- `docs/architecture/gxpm-replacement-architecture.md`
- `docs/architecture/gxpm-v0-contract.md`
- `docs/governance/development-contract.md`
- `CONTEXT.md`
